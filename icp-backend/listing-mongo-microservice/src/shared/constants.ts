import path from 'path';

// PATHS
export const PROJECTDIR = process.cwd();

export const DISTDIR = path.resolve(PROJECTDIR, 'dist');

// export const FRONTENDDIR = path.resolve(PROJECTDIR, 'frontend');
// export const FRONTENDSRC = path.resolve(FRONTENDDIR, 'src');
